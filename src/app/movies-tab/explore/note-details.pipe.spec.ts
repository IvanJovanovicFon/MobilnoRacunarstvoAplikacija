import { NoteDetailsPipe } from './note-details.pipe';

describe('NoteDetailsPipe', () => {
  it('create an instance', () => {
    const pipe = new NoteDetailsPipe();
    expect(pipe).toBeTruthy();
  });
});
